# Podfile for BarsysAppSwiftUI
#
# The SwiftUI rewrite does not *require* any of these pods to build — the
# Bridging/ layer keeps UIKit-only SDKs at arm's length and the mock services
# let you build and run without any of them. Uncomment the pods you need when
# wiring the real backend back in.

platform :ios, '16.0'

target 'BarsysAppSwiftUI' do
  use_frameworks!

  # pod 'SVProgressHUD'
  # pod 'IQKeyboardManagerSwift', '~> 6.5'
  # pod 'FirebaseCore'
  # pod 'FirebaseMessaging'
  # pod 'BrazeKit'
  # pod 'BrazeUI'

end
